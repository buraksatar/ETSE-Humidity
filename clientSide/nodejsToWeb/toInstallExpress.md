```
$ sudo npm install -g express-generator
$ sudo express projectuno
```

About npm start
```
$ sudo netstat -anp tcp || grep 3000
$ sudo kill -9 'number'
```
for all:
```
$ sudo killall -9 node
```
